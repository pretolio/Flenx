/// Shell de navegação do Flenx (widgets Flutter): AppShell responsivo com
/// sidebar/drawer, top bar, perfil e notificações.
///
/// Depende de Flutter — importe SOMENTE no lado web/app (ex.: via
/// `@Import.onWeb`), nunca no servidor (que compila Dart puro, sem Flutter).
library flenx.shell;

export 'shell/shell.dart';
