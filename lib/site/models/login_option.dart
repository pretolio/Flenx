/// Opção dentro do menu/botão de login expansível (ex.: "Entrar", "Criar conta",
/// "Entrar com Google").
class LoginOption {
  const LoginOption({required this.label, required this.href});

  final String label;
  final String href;
}
